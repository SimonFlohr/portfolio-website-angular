import { MagneticButtonDirective } from './magnetic-button.directive';

describe('MagneticButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MagneticButtonDirective();
    expect(directive).toBeTruthy();
  });
});
